mod day_one;
use day_one::*;

mod day_two;
use day_two::*;

mod day_three;
use day_three::*;

// TODO: make the main intelligent, i.e. able to get the day number from command line
// and only run the corresponding function(s). For now this is good enough.
fn main() {
    // day_one(1);
    // day_one(1007);
    // day_two();
    // day_two_part_two();

    //day_three();
    day_three_part_two();
}
